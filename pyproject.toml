[project]
name = "ancserFX"
version = "0.1.0"
description = "StepX Quantitative Futures Trading System"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "Ancser"}]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
